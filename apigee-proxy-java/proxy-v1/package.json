{
  "name": "currency-v1",
  "version": "1.0.0",
  "devDependencies": {
    "apickli": "^2.3.1",
    "cucumber": "^5.1.0"
  },
  "scripts": {
    "deploy": "npx apigeetool deployproxy -u $APIGEE_USER -p $APIGEE_PASS -o $APIGEE_ORG -e $APIGEE_ENV -n 'apigee-v1' -V",
    "test": "npx cucumber-js test"
  }
}
